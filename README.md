# notebook-frontend

A Python package distributing Notebook's static assets only, with no Python dependency.

To publish, make sure you have `toml` and `hatch` installed, and run e.g.:

```bash
python publish.py 7.6.1 4.6.1  # notebook and jupyterlab versions
```
